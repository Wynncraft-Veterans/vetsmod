# --- Gradle ---
.gradle/
build/
out/
classes/

# --- Eclipse ---
*.launch
.classpath
.project
.settings/

# --- IntelliJ ---
.idea/
*.iml
*.ipr
*.iws

# --- VS Code (per-user IDE state, not project config) ---
.vscode/
bin/

# --- macOS ---
*.DS_Store

# --- Fabric ---
run/

# --- Java ---
hs_err_*.log
replay_*.log
sample.log
*.hprof
*.jfr
debug.log
*.log
*.log.gz

# --- Local scratch ---
/.temp/
/temp/

# --- Per-developer gradle extensions (see local.gradle.example) ---
/local.gradle

# --- Claude Code state ---
# Track curated project docs in .claude/, but skip per-user state and ephemeral
# task notes. The README inside ephemeral/ is committed so the convention is
# discoverable; everything else in there is local.
/.claude/settings.local.json
/.claude/projects/
/.claude/ephemeral/*
!/.claude/ephemeral/README.md
# user_profile.md is a Claude auto-memory file (type: user) — describes the
# individual developer, not the codebase. Stays local.
/.claude/user_profile.md
